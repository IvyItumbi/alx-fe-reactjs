# React Query Demo

This project demonstrates use of `react-query` to fetch and cache data.

## Setup

1. `npm install`
2. `npm run dev`

## Features

- Fetches posts from JSONPlaceholder.
- Caches data using `react-query`.
- Refetch button demonstrates data updating.
